# Optional. Leave blank to use the deterministic offline interviewer.
OPENAI_API_KEY=

# Optional. Leave blank to use the server default model.
OPENAI_MODEL=

# Express API port for npm run server:dev and npm start.
PORT=8787
